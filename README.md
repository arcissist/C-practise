afirs.c：声明一个包含6个元素的int数组a，并将它初始化为从2开始的连续6个偶数
asecond.c:编写语句，依次显示a数组中所有的元素，元素间用逗号隔开。
athird.c:定义一个有10个整型元素的数组，这个数组的每一个元素都是通过终端输入的，计算并打印出该数组中所有元素的和
afourth.c:定义一个有10个整型元素的数组，这个数组的每一个元素都是通过终端输入的，计算并打印出该数组中所有的偶数
afifth.c:定义一个有10个整型元素的数组，这个数组的每一个元素都是通过终端输入的，计算并打印出该数组中所有的偶数的和
asixth.c:定义一个有10个整型的数组，这个数组的每一个元素都是通过终端输入的，计算并打印出这10个元素的最大公约数和最小公倍数
aseventh.c:定义一个有10个整型元素的数组，这个数组的每一个元素都是通过终端输入的，要求把这个数组中每一个元素都往后移一个位置，最后一个元素移到第一个元素位置
aeighth.c:定义一个有10个整型元素的数组，这个数组的每一个元素都是通过终端输入的，要求把这个数组中每一个元素都往后移2个位置，最后一个元素移到第2个元素位置
aninthly.c:给定一个5个元素构成的数组，每个元素都在0~9之间，按照位置将其组成一个5位 并且输出
aeleventh.c:定义一个能容纳10个元素的整型数组，前9个元素输人，现在在第五个元素位置插入一个数23后面的数序往后移，然后把结果打印出来验证一下

atwelfth.c:定义一个有10个整型元素的数组，并输入这10个数，删除第3个数，后面剩余的数统一前移一个位置，并把结果打印出来用以验证
athirteenth.c:定义一个有10个整型元素的数组，并输入这10个数，把所有元素中最大元素的下标打印出来
afourteenth.c:定义一个有10个整型元素的数组，并输入这10个数，从数组中找出连续3个元素的子数组，要求这个子数组元素和最大，并输出这个子数组
afifteenth.c：定义一个有10个整型元素的数组，并输入10个数，把这10个数按照从小到大的顺序并排序后的数组打印出来
asixteenth.c:定义一个有10个整型元素的数组，并输入10个数，把这10个元素按照奇数在前偶数在后的顺序重新排列，并且要求奇偶两部分分别有序
二维数组
dfirst.c:矩阵加法：定义两个大小相同的二维数组，并初始化，分别输出两个二维数组的值，以及两个数组相同位置上的值相加以后的结果
dsecond.c:逆序输出二维数组的元素
dthird.c：求二维数组中元素的最大值
dfourth.c:将一个二维数组的进行从小到大排序，每行，每列从小到大排序
dfifth.c：求二维数组的总和和平均值，每行每列的和还有平均值
字符串
sfirst.c：将一个字符数组初始化为“it's a computer”，输出时只显示scomputer
sscend.c:从键盘上接收一个字符串，并计其中字符的个数
